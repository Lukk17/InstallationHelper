---
- name: Install ZSH and curl
  ansible.builtin.apt:
    name:
      - zsh
      - curl
    state: present
  become: yes

- name: Change user shell to ZSH
  ansible.builtin.user:
    name: "{{ ansible_env.USER }}"
    shell: /usr/bin/zsh
  become: yes
