---
# Whether an AUR problem aborts the run on the spot. True keeps going, which is usually what you
# want, because one flaky AUR build should not abandon a forty minute run that has already
# installed a hundred other things.
#
# It does NOT mean the run is reported as successful. Missing AUR packages always mark the play as
# failed at the end, so the exit code tells the truth and the summary names them. Tolerating a
# failure mid-run and pretending it did not happen are different things, and this used to do both:
# it defaulted to true, so the fail task never fired, and nothing else recorded the miss either.
aur_allow_failures: true

# Hard ceiling on a single AUR build, in seconds. The installs run one at a time, so this is per
# package rather than for the batch, and 90 minutes is well clear of the slowest observed build while
# still bounding a paru that has stopped on a prompt none of --noconfirm, --skipreview or
# --noprovides covers. Raise it only if a genuine build is this slow on your machine.
#
# Replaces aur_wait_retries, which paced the old concurrent firing. Serialising the loop removed the
# concurrency and, as a review pointed out, removed the time bound with it, leaving nothing at all to
# stop a hung build on an unattended run.
aur_build_timeout: 5400

# Where vendor .deb files are staged between download and install. Root-owned and mode 0700, so the
# file that apt installs as root cannot be swapped by another local account between the two steps,
# which a predictable name under /tmp allowed.
software_installer_stage_dir: "/var/cache/installation_helper"

# When true, dynamic_install.yaml will purge /var/lib/flatpak/repo if the
# health probe reports an ostree-corruption error. Off by default because
# this is destructive — it removes every installed flatpak runtime.
flatpak_force_repo_reset: false
